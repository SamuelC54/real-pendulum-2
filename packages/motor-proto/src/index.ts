/** Generated types — run **`npm run proto:generate`** from repo root. */
export * from "./gen/motor_pb.js";
/** Sensor types: import `@real-pendulum/motor-proto/gen/sensor_pb.js` (separate file avoids duplicate **Connect** symbol names vs motor). */
