{
  "name": "@real-pendulum/physics-sim",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "python -m cart_pendulum.server --port 58871",
    "start": "python -m cart_pendulum.server --port 58871",
    "test": "python -m pytest tests -q"
  }
}
