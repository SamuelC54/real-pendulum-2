/**
 * Shadcn line chart — use ChartContainer + LineChart from here (recharts is encapsulated).
 * @see https://ui.shadcn.com/docs/components/chart
 */
export { CartesianGrid, Line, LineChart, XAxis, YAxis } from "recharts";
export {
  type ChartConfig,
  ChartContainer,
  ChartLegend,
  ChartLegendContent,
  ChartTooltip,
  ChartTooltipContent,
} from "@/components/ui/chart";
